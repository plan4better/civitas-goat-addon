---
# Pinned versions for this addon's external dependencies.
goat_addon_software:
  chart_version: "0.1.0"
  app_version_pin: ""   # empty = chart's appVersion default; override to lock a specific GOAT image tag
